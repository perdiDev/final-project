[property]
net-scale-factor=0.0039215697906911373
cluster-mode=2

# Lokasi model ONNX
onnx-file=../models/yolov8n_coco.onnx
model-engine-file=../models/yolov8n_coco.onnx_b1_gpu0_fp16.engine

# Format warna: 0 = RGB, 1 = BGR
model-color-format=0

# Jumlah kelas (YOLOv8n = 80 kelas COCO)
num-detected-classes=80
labelfile-path=../labels/labels_coco.txt

# Interval inferensi (0 = setiap frame)
interval=0

# ID unik untuk plugin ini
gie-unique-id=1

# Nama blob output dari model ONNX
output-blob-names=output

# Batch size untuk inferensi
batch-size=1

# Mode network: 0 = FP32, 1 = INT8, 2 = FP16
network-mode=2

# Arahkan ke library parser yang sudah dikompilasi (PATH ABSOLUT)
custom-lib-path=../lib/libnvdsinfer_custom_impl_Yolo.so

# Nama fungsi parser yang akan dipanggil
parse-bbox-func-name=NvDsInferParseYolo

[class-attrs-all]
nms-iou-threshold=0.45
pre-cluster-threshold=0.35
